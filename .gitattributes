# Auto detect text files and perform LF normalization
* text=auto

# Keep images binary (avoid CRLF corruption from text=auto)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
